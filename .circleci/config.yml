version: 2.1

jobs:
  docker-build-and-push:
    docker:
    - image: vault
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run: |
          apk add --no-cache vault
          make docker-login
          make docker-build
          make docker-push
